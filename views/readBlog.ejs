<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blogMaterial.title %> - Read</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div class="blog-container">
        <h2>Blog Details</h2>

        <!-- Display Blog Title -->
        <div class="blog-title">
            <h3><%= blogMaterial.title %></h3>
        </div>

        <!-- Display Creator Information -->
        <div class="creator-info">
            <p><strong>Creator:</strong> <%= creatorDetails.fname %> <%= creatorDetails.lname %></p>
        </div>

        <!-- Display Publish Date -->
        <div class="publish-info">
            <p><strong>Category:</strong> <%= instance.category %></p>
            <p><strong>Publish Date:</strong> <%= new Date(instance.publishDate).toLocaleDateString() %></p>
        </div>

        <!-- Display Blog Content -->
        <div class="blog-content">
            <p><%= blogMaterial.content %></p>
        </div>

        <!-- Check if Blog Has an Image -->
        <div class="blog-image">
            <% if (blogMaterial.imageURL && blogMaterial.imageURL !== 'no image') { %>
                <img src="<%= blogMaterial.imageURL %>" alt="Blog Image" width="400">
            <% } else { %>
                <p>No image available</p>
            <% } %>
        </div>

        <!-- Like Button -->
        <a href="/bps/do-like/<%= instance._id %>/<%= instance.creatorId %>">
        <div class="like-button">
            <% if (liked) { %>
                <i class="fa-solid fa-thumbs-up"></i>
            <% } else { %>
                <i class="fa-regular fa-thumbs-up"></i>
            <% } %>
        </div>
        </a>

        <!-- Display Visibility and Keywords -->
        <div class="visibility-info">
            <p><strong>Keywords:</strong> <%= instance.keywords.join(', ') %></p>
        </div>
    </div>
</body>
</html>
